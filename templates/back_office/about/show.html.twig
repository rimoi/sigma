{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ about.title }}</td>
            </tr>
            <tr>
                <th>BackgroundImage</th>
                <td>{{ about.backgroundImage }}</td>
            </tr>
            <tr>
                <th>FirstFile</th>
                <td>{{ about.firstFile }}</td>
            </tr>
            <tr>
                <th>SecondFile</th>
                <td>{{ about.secondFile }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ about.description }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ about.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_office_about_index') }}">back to list</a>

    <a href="{{ path('app_back_office_about_edit', {'id': about.id}) }}">edit</a>

    {{ include('back_office/about/_delete_form.html.twig') }}
{% endblock %}
