{% extends 'base_admin.html.twig' %}

{% block title 'Nouvelle Page A propos' %}

{% block body %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Accueil</a></li>
        <li class="breadcrumb-item"><a href="{{ path('admin_dash_board_index') }}">Dashboard</a></li>
        <li class="breadcrumb-item">
            <a href="{{ path('app_back_office_about_index') }}">A propos</a>
        </li>
        <li class="breadcrumb-item active">{{ block('title') }}</li>
    </ol>

    <div class="card shadow mb-4">
        <div class="card-header py-3 px-2">
            <h6 class="m-0 font-weight-bold text-primary text-center">{{ block('title') }}</h6>
        </div>
        {% include 'back_office/about/_form.html.twig' %}
    </div>
{% endblock %}